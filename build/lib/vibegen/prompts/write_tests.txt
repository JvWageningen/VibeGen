TASK
Your job is to write pytest test cases for the module described below.
Use the test plan and source code provided — do NOT invent packages or imports.

MODULE
{{module_path}}

SPECIFICATION
{{spec}}

TEST PLAN (derived from actual source code)
{{test_plan}}

SOURCE FILES (actual generated code — base your imports on this)
{{source_files}}

DEPENDENCY GRAPH
{{dependency_graph}}

INSTALLED DEPENDENCIES (the ONLY packages available — never import anything else)
{{installed_deps}}

CONSTRAINTS
{{constraints}}

IMPORT RULES
- Always import from the installed package name, never from src/:
    Correct : from {{package}}.module import SomeClass
    WRONG   : from src.{{package}}.module import SomeClass
- Only import external packages that appear in INSTALLED DEPENDENCIES.
- Use `pytest` for all test infrastructure.

OUTPUT FORMAT
Return one or more file blocks in the following format:

--- file: <path> ---
<file contents>

Separate multiple files with the same delimiter. Paths are relative to the repo root.
Do NOT wrap the output in markdown fences or add any extra text.

Example:
--- file: tests/test_<module>.py ---
# pytest code here
