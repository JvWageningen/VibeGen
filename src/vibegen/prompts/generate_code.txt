think hard

## SCOPE
Implement COMPLETE, WORKING Python source code for every module in the plan below.
Only implement features described in the SPECIFICATION — no extras, no speculative utilities.
Do NOT return a plan, summary, or description. Write full implementation code only.

## ANCHOR

IMPLEMENTATION PLAN
{{plan}}

SPECIFICATION
{{spec}}

REPO STRUCTURE
{{repo_tree}}

CONSTRAINTS
{{constraints}}

## OUTCOME
Acceptance criteria — your output passes when ALL of the following hold:
1. Every module listed in the plan has a corresponding `--- file: src/{{package}}/... ---` block.
2. Every function has a complete body (no `pass`, `...`, stubs, or ellipses).
3. Every public function has type hints and a Google-style docstring.
4. All imports use the installed package name: `from {{package}}.module import X`
   (NEVER `from src.{{package}}.module import X`).
5. No module imports a package not present in CONSTRAINTS or stdlib.

FILE PLACEMENT RULES
- Source code: `src/{{package}}/<module>.py`
- Tests: `tests/test_<module>.py`
- Paths are relative to the repo root.

DO NOT WRITE THESE FILES (already exist — overwriting will break the project):
- pyproject.toml, README.md, CLAUDE.md
- .vscode/settings.json, .gitignore, .gitattributes, .pre-commit-config.yaml
- Any .toml, .yaml, .json, or .md file

OUTPUT FORMAT
Return file blocks only, using this exact delimiter:

--- file: <path> ---
<complete file contents>

Separate multiple files with the same delimiter. No markdown fences around the output.

Example:
--- file: src/{{package}}/__init__.py ---
"""Package init."""

--- file: src/{{package}}/main.py ---
"""Main module."""
from __future__ import annotations
# full implementation here
