The user has reviewed the generated spec and requested these changes:

{{changes}}

Update the spec to incorporate these changes. Return the COMPLETE updated spec — not just the changed parts.

OUTPUT FORMAT:
Wrap the complete updated spec in these exact delimiters:

--- spec.md ---
(complete updated spec content here)
--- end ---
