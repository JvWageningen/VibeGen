think hard

## SCOPE
Analyse the actual source code below and produce a detailed test plan.
Do NOT write any code. Return only the plan text.

## ANCHOR

SPECIFICATION
{{spec}}

INSTALLED DEPENDENCIES
{{installed_deps}}

SOURCE FILES
{{source_files}}

DEPENDENCY GRAPH
{{dependency_graph}}

CONSTRAINTS
{{constraints}}

## OUTCOME
Your test plan is complete when ALL of the following hold:
1. Every public function/class has at least 3 test cases specified.
2. Import statements use the correct installed package name (from INSTALLED DEPENDENCIES).
3. Mock strategy is specified for every external dependency.
4. At least one edge case and one failure mode per module are covered.

Return a plain-text test plan with these sections:

## Test Strategy
Brief overview of the testing approach (mocking strategy, fixture needs, etc.).

## Test Files
List each test file to create under tests/ with a one-line purpose.

## Test Cases Per Module
For each module, list the test cases:
  - function/class being tested
  - scenario
  - key assertion(s)
  - fixtures or mocks needed

## Correct Import Statements
List the exact import lines tests should use. Use ONLY packages from INSTALLED DEPENDENCIES.
Example:
  from {{package}}.processor import count_words
  import pytest

## conftest.py Fixtures
List shared fixtures needed (name, type returned, purpose).

## Notes
Any important details: mock points, test-data requirements, known edge cases.
