think

## Scope
Show all usages of: $ARGUMENTS
Do NOT modify any code.

## Anchor
Run `cymbal trace $ARGUMENTS` (call graph down) and `cymbal refs $ARGUMENTS` (references). Fallback: search manually.

## Outcome
| File | Line | Caller/Callee | Relationship | Return Value Usage |
|------|------|---------------|--------------|-------------------|
| vibegen/_pipeline.py | 87 | _generate_code | calls | assigned to `result` |

Relationship: calls / imports / inherits / overrides
Note the most frequent call sites, unusual usages, and whether test coverage exists.
