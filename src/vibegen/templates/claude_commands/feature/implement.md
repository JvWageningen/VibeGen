Implement this step from the plan: $ARGUMENTS

1. Read PLAN.md and relevant source files
2. Implement exactly what the step describes - no more, no less
3. Run: uv run ruff check . --fix && uv run ruff format .
4. Run: uv run pytest -x --tb=short