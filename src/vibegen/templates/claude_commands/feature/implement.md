think hard

## Scope
Implement this step from the plan: $ARGUMENTS
Do NOT implement other plan steps. Do NOT add features beyond what the step describes.

## Anchor
Read PLAN.md and locate the step matching $ARGUMENTS. Read all source files the step references before writing any code.

## Outcome
Your implementation is complete when ALL of the following hold:
1. The step is implemented exactly as described — no more, no less.
2. Type hints and Google-style docstrings are present on all new code.
3. `uv run pytest -x` passes.
4. `uv run ruff check . --fix` and `uv run mypy src/` pass.
