Add functionality to: $ARGUMENTS

1. Read the module and its tests
2. Implement following existing patterns, with type hints and docstrings; export from __init__.py if public
3. Add or update tests
4. Run: uv run ruff check . --fix && uv run ruff format .
5. Run: uv run pytest -x --tb=short