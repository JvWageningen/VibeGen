Document environment variables and configuration.

1. Search all source files for os.environ, os.getenv, dotenv references, and config loading
2. Create or update docs/ENV.md with: variable name, description, required/optional, default value, example
3. Flag any values that are hardcoded but should be environment variables