Generate or update CHANGELOG.md: $ARGUMENTS (version number, e.g. 1.2.0)

1. Run: git log --oneline --no-merges to review commits since the last tag
2. Group commits into: Added, Changed, Fixed, Removed
3. Write or prepend a version section in CHANGELOG.md using Keep a Changelog format (https://keepachangelog.com)