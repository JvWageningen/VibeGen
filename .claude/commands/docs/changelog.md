## Scope
Generate or update CHANGELOG.md for version: $ARGUMENTS
Do NOT modify source code.

## Anchor
Run `git log --oneline --no-merges` since last tag.

## Outcome
Write or prepend a version section in CHANGELOG.md using Keep a Changelog format.
Group commits into: **Added**, **Changed**, **Fixed**, **Removed**.
